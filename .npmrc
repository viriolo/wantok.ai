# Remove engine-strict to allow more flexibility
# engine-strict=true
# Use a more flexible Node.js version requirement
# node-version=18.17.0

# Add this to ensure pnpm uses the correct Node.js version without failing
use-node-version=18.17.0
auto-install-peers=true
strict-peer-dependencies=false
